dbt_profile_name_in_dbt_project_file: # update
  outputs:
    dev:
      dataset: bigquery_dataset # update
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: /key/file/path # update
      location: EU
      method: service-account
      priority: interactive
      project: gcp_project_id # update
      threads: 4
      type: bigquery
  target: dev
